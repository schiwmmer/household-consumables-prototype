# 囤耗材 · UI 设计系统 Spec

> 立项：家庭耗材管理微信小程序 MVP
> 版本：v1.0 · 2026-07-31

---

## 1. 设计语言一句话

**轻快俏皮 × 纸质笔记本** — 给带娃双职工的减法工具，像翻一本记满日用品的备忘手账，温暖、不焦虑、带一点点童趣。

---

## 2. 设计方向锚点

| 维度 | 选择 | 反面 |
|------|------|------|
| Tone | 纸质手账、轻快俏皮 | 不选：极简冷淡、工业监控、科技霓虹 |
| 情绪 | 温暖 / 安心 / 不焦虑 | 不选：严肃、高效冰冷、儿童卡通 |
| 主要材质 | 米色纸张、铅笔线条、印章式饱和点缀 | 不选：玻璃质感、毛金属、发光线条 |
| 主角 | 大号衬线余量数字 + 铅笔手绘图标 | 不选：大图标+圆角卡片模板 |

---

## 3. 色彩系统

### 3.1 主色板（oklch，倾向暖纸调）

| Token | oklch | HEX 近似 | 用途 |
|-------|-------|----------|------|
| `--paper-base` | `oklch(0.96 0.02 85)` | `#F7F0E3` | 页面背景（米黄纸） |
| `--paper-card` | `oklch(0.99 0.01 85)` | `#FFFEF9` | 卡片底色（暖白） |
| `--ink` | `oklch(0.18 0.02 70)` | `#2E2318` | 主文字（深胡桃） |
| `--ink-soft` | `oklch(0.38 0.02 70)` | `#6B5D4F` | 次级文字 |
| `--brand-butter` | `oklch(0.72 0.14 75)` | `#E8B84A` | 品牌/辅助黄（鹅黄） |
| `--brand-coral` | `oklch(0.65 0.18 35)` | `#E86A54` | 重点橙（珊瑚） |

### 3.2 状态色

| Token | oklch | HEX | 语义 |
|-------|-------|-----|------|
| `--status-green` | `oklch(0.70 0.14 145)` | `#5BBF8A` | 余量充足 |
| `--status-yellow` | `oklch(0.78 0.14 85)` | `#E8B84A` | 偏低提醒 |
| `--status-red` | `oklch(0.62 0.19 25)` | `#E85E4A` | 即将用完 |

> 注意：主背景**绝不**用纯白 #FFF，纯白 + 白纸感矛盾；纯黑同理不用于文字。

### 3.3 用色比例

- 90% 暖纸色（`paper-base` + `paper-card`）
- 6% 中性墨色（文字）
- 3% 珊瑚主 CTA（`brand-coral`）
- 1% 鹅黄（Quiet 辅助徽标）
- 状态色只在色点（小圆点 8rpx）和数字颜色出现

---

## 4. 字体

### 4.1 字体配对

| 层 | 字体栈 | 理由 |
|----|--------|------|
| **展示/数字** | `Songti SC`, `Source Serif Pro`, `Source Han Serif`, `serif` | 衬线营造纸本手账品牌感，数字用衬线有温度。系统后备：iOS Songti SC；Android Droid Serif |
| **正文** | `-apple-system`, `PingFang SC`, `Microsoft YaHei`, `sans` | 中文正文用无衬线清晰度 |
| ISystem 标题 | `--body-bold` | 28rpx PingFang bold |

> 与 AI Slop 反例对照：**不选 Inter/Roboto/Arial/Open Sans**；**不选等宽当技术感**；**不选大图标+圆角标题模板**。

### 4.2 字号（模块化比例 major-third 1.25，用 clamp() 流体）

| Token | clamp() | 屏宽 375 时 |
|-------|---------|-------------|
| `--display` | `clamp(56rpx, 12vw, 88rpx)` | 88rpx（首页大字） |
| `--title-lg` | `clamp(32rpx, 5.5vw, 44rpx)` | 44rpx |
| `--title` | `clamp(28rpx, 4.8vw, 36rpx)` | 36rpx |
| `--body` | `clamp(26rpx, 3.8vw, 30rpx)` | 30rpx |
| `--caption` | `clamp(20rpx, 3vw, 24rpx)` | 24rpx |

行高：display 1.0、title 1.15、body 1.5、caption 1.4。

### 4.3 字重管理

- 数字（剩余天数 / 价格）：**bold 700** + serif，视觉锚点
- 标题：**semibold 600**
- 正文：**regular 400**

禁止：不出现 font-weight 300 细（米黄底上细字不可读）；不出现 900 极致粗（破坏纸本柔感）。

---

## 5. 空间系统

### 5.1 基础单元

8rpx（小程序 750rpx 稿下 1rpx = 0.5pt）。所有间隔、圆角、线宽的倍数都是 8rpx。

### 5.2 卡片布局（非卡片套卡片）

**首页卡片**采用单幅纸片 + 差异化圆角打破单调：

| 属性 | 值 |
|------|-----|
| 圆角 | `0 48rpx 48rpx 48rpx`（左上 0 = 贴边纸片角） |
| 内边距 | `clamp(24rpx, 4vw, 40rpx)` |
| 阴影 | `0 2rpx 12rpx oklch(0.18 0.02 70 / 0.06)`（淡墨影，非通用投影） |
| 卡片间距 | 24rpx（两卡之间一行的"字距"） |
| 底分隔线 | 不使用 border-bottom，卡片下留白 = 分隔 |

> 与 AI Slop 反例对照：**不做卡片套卡片、不做居中一切、不做英雄指标模板**。卡片布局左右就放品牌+品名、右边放天数色点，力量对比像手账侧页标注。

### 5.3 页面级留白

- 页面左右边距：`clamp(32rpx, 5vw, 48rpx)`
- 顶部 tab 到首卡：24rpx
- 段落间：24rpx / 40rpx 交替（节奏变化，不平均）

---

## 6. 组件库

### 6.1 首页余量卡片

```
┌────────────────────────────────────────┐
│ [珊瑚印章圆点] 德宝 · 抽纸            ← caption + title
│                                       │
│   客厅/白色柜顶         ┌────┐        │
│   ~ 剩余              │ 9d │ ← display serif + 状态色
│   ──────────           └────┘        │
│   ████████████░░ 68%                  ← 进度条黄纸质感
└────────────────────────────────────────┘
```

差异化：**奇数卡圆角朝左上=0，偶数卡圆角朝右上=0**（打破完全镜像的 AI 味）。

### 6.2 状态色点

- 8rpx 正圆，圆心偏移卡片左上角外 -8rpx（"图钉" 感觉）
- 绿 / 黄 / 红按库存状态
- 黄色点小幅度循环呼吸动效（opacity 0.7→1→0.7，4s loop，ease-in-out）

### 6.3 CTA 按钮

| 类型 | 样式 |
|------|------|
| Primary | 珊瑚 `brand-coral` 底 + 白字 + 0 圆角 + 500字重 + 160rpx 高 — 像邮戳 |
| Secondary | 米黄底 + 细胡桃描边 + 0 圆角 — 像便签纸边 |
| 文字链 | `brand-coral` 文字 + 底部 2rpx 珊瑚下划线 |
| FAB | 右下角 + 56rpx 圆 + 白底 + 珊瑚「+」+ 阴影= `0 4rpx 16rpx oklch(0.18 0.02 70 / 0.15)` |

按钮不做通用圆角，0rpx 圆角营造纸/墨的刚硬棱角。

### 6.4 规格标签（详情页规格条目）

每项规格 = 一行米白带圆角 8rpx 标签，左 `spec_name` 灰、右 `value + unit` 珊瑚，内联排列。（不套卡片，避免嵌套）

### 6.5 输入框

| 状态 | 样式 |
|------|------|
| 默认 | 无边框底 + 下划线（1rpx dashed `--ink-soft`） |
| 聚焦 | 粗下划线（3rpx solid `--brand-coral`） |
| 错误 | 底画短线抖动 + 红字提示 |

避免输入框全包圆角边框，那是通用模板的恶习。

### 6.6 空状态

- 铅笔插画（约 120×120rpx）：空抽屉或空购物篮
- 标题：`还没有耗材 ` + 副标题：`快翻包纸巾，点右下角把它记下来`
- 比如直接用 emoji 代替插画也是可以的：先 📝 / 🧻 / 🫧 让第一稿看得见

---

## 7. Icon 语言

铅笔线条 SVG（stroke only，fill 不用），线宽 2.5px，stroke-linecap round。

| 语义 | 图标描述 |
|------|----------|
| 首页 | 横放记事本 |
| 扫码 | 方框 + 对角扫描线 |
| 我的 | 折角笔记本 |
| 纸巾 | 卷纸横截面 |
| 洗护 | 倒置瓶子 |
| 化妆品 | 口红侧影 |
| 存储 | 抽屉 |
| 位置 | 图钉 |
| 已用完 | 卷纸用尽的芯筒 |

> 全部用铅笔线描，拒绝通用线性图标库的 AI Slop 感。图标尺寸：导航 40rpx、卡片内 32rpx。

---

## 8. 动效规范

| 场景 | 属性 | Duration | Easing |
|------|------|----------|--------|
| 页面转场 | opacity + translateX | 220ms | `cubic-bezier(0.22, 1, 0.36, 1)` (ease-out-expo) |
| 色点呼吸 | opacity | 4000ms loop | ease-in-out |
| 滑杆调整数 | 字号变化 | 180ms | ease-out-expo |
| 卡片出现 | translateY + opacity | 280ms | ease-out-expo，stagger 40ms/卡 |
| 空状态插画 | 上下微浮动 | 3000ms loop | sine-in-out |

> 与 AI Slop 反例对照：**不动画化布局属性（width/height/grid）；不用回弹/弹性缓动**。状态变化动效即可。

---

## 9. 响应式规则

- 基于 375pt iPhone X 基准，使用 `clamp(min, vw, max)` 自适配
- 字号 ≥ 20rpx（`--caption`），不设置更低
- 卡片左右安全区：32~48rpx（刘海屏/Home 指示器）
- 大号手机（≥ 414pt）不重复排列——保持单列

---

## 10. 暗色模式（可选 V2）

如果暗色模式：黑底 ≠ #000，采用 oklch(0.12 0.02 70)（暖墨黑），纸张 oklch(0.20 0.02 70) / oklch(0.28 0.02 70)，珊瑚色 oklch(0.55 0.15 35)（降低亮度）。主调从纸本感转为夜光手账感。

---

## 11. 验收条件（交前端）

| # | 条件 | 通过判定 |
|---|------|----------|
| AC-1 | 字体配对**不含** Inter / Roboto / Arial / Open Sans | grep 字体栈，确认 |
| AC-2 | 页面背景**非**纯白 #FFF / #FFFFFF | 查 paper-base HEX |
| AC-3 | 首页卡片**不是**统一圆角（至少两类不同圆角打破节奏） | 视觉检查 |
| AC-4 | 状态色仅出现在色点 & 数字，全身无整色块背景 | 视觉检查 |
| AC-5 | 卡片**不**套卡片（首页卡片内无子卡片，规格条目不嵌套） | DOM 检查 |
| AC-6 | Primary button 圆角 = 0rpx | border-radius 检查 |
| AC-7 | 动效不含 spring / bounce / elastic | transition-timing-function |
| AC-8 | 字号 clamp() 至少覆盖 display / body / caption 三类 | CSS 检查 |
| AC-9 | 至少一个地方出现"铅笔插画或 emoji 占位" | 视觉检查 |
| AC-10 | 空状态文案不含"暂无数据""空空如也"等 AI 高频词 | 文案 grep |

---

_文档生成：ui-designer impeccable skill，2026-07-31_
